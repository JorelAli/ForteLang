{
    # Adds two parameters together
    add = x -> y -> x + y;

    # Square a number
    SQUARE = x -> x * x;

    # Example with guards
    max = x -> y -> 
        | x > y ->> x
        | ->> y;
}